docker-build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$DOCKER_USERNAME" -p "DOCKER_TOKEN"

  script:
    - echo ' @@@ Building Images ..... '
    - docker build --tag dudaferreira/tw-todos-gha
    - docker push dudaferreira/tw-todos-gha
    - echo 'Done -- image ok !'